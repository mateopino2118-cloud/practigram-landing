# Postgres — Railway lo provee automáticamente como variable de entorno
# cuando linkeas el servicio de DB al proyecto. Copiá el valor desde
# Railway → tu proyecto → Variables → DATABASE_URL
DATABASE_URL=postgresql://user:pass@host:5432/dbname
